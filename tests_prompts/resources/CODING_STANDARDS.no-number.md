# Coding Standards

## Commits

- Commit subject MUST be a short imperative description (e.g. `add fizzbuzz helper`). The full conventional-commits prefix is NOT required.
- Do NOT include the issue number in the commit subject.
- Never use the `RALPH:` prefix.

## Quality gates

- pytest: `uv run pytest`
