# Implementation — borrow-gentle-orchestrator

## Commits
- 65f5273 — task 1: Tighten change-orchestrator.md with explicit start/resume route, artifact-change invalidation reopening review on resume/compaction, session-scoped (phase, task_fingerprint) duplicate-launch guard, exact SKILL.md path injection (`Skills to load before work`), auto/interactive session mode with phase gates, and preserved parent-decomposition carve-out + CLI/state authority; tests: `uv run pytest tests/test_renderers.py tests/test_change.py -q` (106 passed; 1 pre-existing failure unrelated to the prompt edit: `test_change_orchestrator_meta_declares_primary_restricted_agent` asserts `openai/gpt-5.5` vs `minimax/MiniMax-M3`; verified pre-existing with `git stash`). Targeted gate + prompt-set lock tests pass: `test_change_orchestrator_body_has_human_review_gate_heading`, `test_change_orchestrator_body_gate_names_every_artifact`, `test_change_orchestrator_body_gate_requires_explicit_confirmation`, `test_change_orchestrator_body_gate_invalidates_on_artifact_change`, `test_change_orchestrator_body_gate_carves_out_parent_decomposition`, `test_change_orchestrator_body_gate_encodes_resume_semantics`, `test_change_orchestrator_description_unaffected_by_body_only_gate`, `test_change_agent_prompt_set_contains_expected_contract_keywords`.
- 342e3a2 — task 2: Normalize change-explorer.md to the shared phase result envelope (status/artifacts/summary/semantic_facts/skills); move budget + follow-up under semantic_facts with budget also written canonically into exploration.md prose; require skills: loaded|fallback|none + skill_resolution when degraded; preserve blocked-with-missing-input-before-block policy via semantic_facts.blocked_reason; tests: `uv run pytest tests/test_renderers.py::test_change_agent_prompt_set_contains_expected_contract_keywords -v` (passed); full renderers + change suite remains 106 passed with the same pre-existing model-naming failure; `budget` keyword still present in change-explorer.md.
- f78a82f — task 3: Normalize change-implementor.md to the shared phase result envelope; partial becomes a semantic_facts value (semantic_facts.partial + remaining_tasks + changed_files); require skills: loaded|fallback|none + skill_resolution; preserve one-commit-per-task + implementation.md write discipline + task-next/task-done sequencing; blocked emits brief prose note before block plus semantic_facts.blocked_reason; tests: `uv run pytest tests/test_renderers.py -q` (99 passed; same pre-existing model-naming failure); `task-next` and `task-done` keywords present in change-implementor.md.
- bc607b0 — task 4: Normalize change-validator.md to the shared phase result envelope; verdict + critical move under semantic_facts and stay also in validation.md prose; require skills: loaded|fallback|none + skill_resolution; missing semantic facts surface as verdict: fail or status: blocked, never silent pass; preserve blocking policy B (CRITICAL-only) + archive routing pass / pass-with-warnings-with-zero-critical / fail-and-back-to-implement bounded by CHANGE_FIXUP_MAX_ITERATIONS; tests: `uv run pytest tests/test_renderers.py -q` (99 passed; same pre-existing model-naming failure); `verdict` and `task-list` keywords present in change-validator.md.
- aec09f3 — task 5: Update docs/design/change-orchestrator.md to mirror tasks 1-4: explicit start/resume route contract, artifact-set bound approval with session-gap/compaction reopen, shared result envelope with per-phase semantic facts and prose recovery, delegation launch log with refusal wording and prose fallback, exact SKILL.md path injection + `Skills to load before work` handoff + skill_resolution reporting, session mode (auto/interactive) phase gate with pause-before-implementation and auto-continues-only-when-safe; tests: `uv run pytest tests/test_renderers.py tests/test_change.py -q` (106 passed; same pre-existing model-naming failure unrelated to this docs edit).
- 01138c2 — task 6: Add seven render-contract tests locking the borrowed disciplines — start/resume route + disk authority, artifact-set bound approval, (phase, task_fingerprint) duplicate-launch guard, exact SKILL.md path injection, auto/interactive phase gate, shared phase result envelope across explorer/implementor/validator with the right phase-specific semantic facts, and a change-orchestrator frontmatter parity guard; tests: `uv run pytest tests/test_renderers.py tests/test_change.py -q` (113 passed, up from 106; same pre-existing model-naming failure unrelated to the new tests).

## Remaining
- none — change complete.
