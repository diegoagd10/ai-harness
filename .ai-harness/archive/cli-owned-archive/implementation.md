# Implementation — cli-owned-archive

## Commits
- 98f997d — task 1: feat(change): add CLI-owned change-archive command with structural preflight and transactional moves; tests: `uv run pytest tests/test_change.py`
- 808d3c2 — task 2: feat(agents): add change-archiver prompt and wire renderer/wizard/install registrations; tests: `uv run pytest tests/test_renderers.py tests/test_install.py tests/test_set_models.py`
- 8863145 — task 3: feat(orchestrator): route archive to change-archiver as terminal flow; tests: `uv run pytest tests/test_renderers.py`

## Remaining
- none