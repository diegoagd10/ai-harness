# Skill Registry

This project uses the installed ai-harness skills from the active OpenCode environment.

Required SDD implementation skills:

- `read-task-spec`
- `tdd-implement`
- `coding-guidelines`

SDD planning phases may run without additional skills unless the orchestrator payload requires them.
